<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SeatBites</title>
    <!-- talwwind css -->
    <link
      href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
    <!-- compiled css -->
    <link rel="stylesheet" href="/css/app.css" />
    <!-- google fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap"
      rel="stylesheet"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins&display=swap"
      rel="stylesheet"
    />
    <script src="https://kit.fontawesome.com/1abcfa3763.js" crossorigin="anonymous"></script>
    
  </head>
  <body>
    <!-- nav bar -->
    <nav class="container mx-auto flex items-center justify-between py-4">
      <div>
        <a href="/"
          ><img src="/img/logo.png" alt="logo" class="w-1/2 ml-4"
        /></a>
      </div>
      <div>
        <ul class="flex items-center text-lg">
          <li class="ml-6"><a href="/#menu">Menu</a></li>
          <% if(user) { %>
            <% if(user.role == "admin") { %>
              <li class="ml-6"><a href="/admin/orders">Orders</a></li>
            <% } %>
            <% if(user.role == "customer") { %>
              <li class="ml-6"><a href="/customer/orders">Orders</a></li>
            <% } %>
          
          <li>
          
            <li class="ml-6">
              <form id="logout" action="/logout" method="POST">
                <a onclick="document.getElementById('logout').submit()" href="#">Logout</a>
              </form>
            </li>
            <!-- <a href="/logout">Logout</a> -->
          </li>
          <% } else { %>
          <li class="ml-6"><a href="/register">Register</a></li>
          <li class="ml-6"><a href="/login">Login</a></li>
          <% } %>
          <!-- <% if(true){ %>
            <h1>foo</h1>
          <% } else{ %>  
            <h1>bar</h1>
         <% } %> -->
         <% if(user) { %>
          <% if(user.role == "admin"){ %>
            <li class="ml-6">
              <a
                href="#"
                class="inline-block px-3 py-2 rounded-full flex items-center mr-4"
              >
                <img src="/img/chef.png" alt="logo"
              /></a>
            </li>
            <% } %>
         <% if(user.role == "customer") { %>
          <li class="ml-6">
            <a
              href="/cart"
              class="inline-block px-5 py-3 rounded-full flex items-center mr-4"
            >
              <span id="cartCounter" class="text-white font-bold pr-2"
                ><%= session.cart ? session.cart.totalQty : '' %></span
              >
              <img src="/img/cart.png" alt="logo"
            /></a>
          </li>
          <% } %>
          <% } else{ %>  
            <li class="ml-6">
              <a
                href="/cart"
                class="inline-block px-5 py-3 rounded-full flex items-center mr-4"
              >
                <span id="cartCounter" class="text-white font-bold pr-2"
                  ><%= session.cart ? session.cart.totalQty : '' %></span
                >
                <img src="/img/cart.png" alt="logo"
              /></a>
            </li>
         <% } %>
         
         
         
        </ul>
      </div>
    </nav>
    <%- body %>
    <!-- <div class="footer-basic">
      <footer>
          <div class="social">
              <a href="#"><i class="iconlast fab fa-instagram"></i></a>
              <a href="#"><i class="iconlast fab fa-facebook"></i></a>
              <a href="#"><i class="iconlast fab fa-twitter"></i></a>
              <a href="#"><i class="iconlast fas fa-envelope"></i></a>
          </div>
          <p class="copyright">SeatBites © 2021</p>
      </footer>
  </div> -->
    <!-- complied js -->
    
    
    <script src="/socket.io/socket.io.js"></script>
    
      
    
    <script src="/js/app.js"></script>
  </body>
</html>